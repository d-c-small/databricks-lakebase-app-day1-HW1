# Expected environment variables for this application.
# LAKEBASE_URL  — Full PostgreSQL connection URL, injected automatically from
#                 the Databricks secret bound in app.yaml.
# FLASK_SECRET_KEY — Signs Flask sessions/flash messages. A random key is
#                    generated at startup if this is not set (not safe for prod).
